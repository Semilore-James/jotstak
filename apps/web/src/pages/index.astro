---
// Landing page. Must render a real Jotstak document above the fold within 3s of scroll.
// Cream, terracotta, Lora serif, hand-drawn accents. No generic SaaS layout.
// TODO(M4): install command, once the Marketplace publisher ID exists.
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Jotstak — one file. Prose, tables and diagrams.</title>
    <meta
      name="description"
      content="Stop scattering a single PRD across Docs, Sheets and Miro. Write it in .jot — prose, tables and diagrams in one plain-text file, rendered as a warm, hand-kept notebook."
    />
  </head>
  <body>
    <main>
      <!-- TODO(v1): hero with a rendered .jot sample above the fold -->
      <h1>One file. Prose, tables and diagrams.</h1>
      <p>
        Stop scattering a single PRD across Docs, Sheets and Miro. Write it in
        <code>.jot</code> — and get a document that's actually worth re-reading.
      </p>
      <p>
        <code>.jot</code> is a superset of Markdown, so any <code>.md</code> file
        renders unchanged. Paste one in and see.
      </p>
      <a href="/playground">Try the playground</a>
      <a href="/docs">Read the docs</a>
      <!-- TODO(v1): gallery, "why Jotstak", Buttondown email capture -->
    </main>
  </body>
</html>
