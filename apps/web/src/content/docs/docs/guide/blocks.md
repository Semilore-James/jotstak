---
title: "Blocks, bodies and nesting"
description: "Every block takes the same shape, and any block can hold another."
draft: true
sidebar:
  order: 2
---

<!-- SHELL PAGE. An outline of what this page has to do, not the finished
     documentation. `draft: true` keeps it out of the built site until it is
     written. Delete the flag and this comment when it is. -->

Establish that blocks compose, so the reader stops looking for a list of allowed combinations. A metric inside a decision inside a panel is not a special case; it is the only case.

## What this page has to cover

- A directive line, then an indented body
- `key: value` fields versus free prose in a body
- Nesting: a block inside a block, to any depth
- What nesting does NOT change (the ruling, the widths)
- When to reach for `@panel` instead of a named card
